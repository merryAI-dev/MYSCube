# Release 805 임시저장·필수정책·상태 재조사

## 결론과 확인 범위

배포 `8401b966db843804956144893584527b919c8622`를 별도 추출하고 **새 운영 읽기 자료로 재실행**했다. 이전804 결과를 현재 사실로 재사용하지 않았다. 새 저장 이력과 인증 저장자·시각이 실제 운영 Firestore에 생긴 것은 확인했으나, 기존 모든 초안의 과거 이력이 복원된 것은 아니다. 상태와 필수값도 기존 데이터의 불일치가 남아 있다.

- 새 snapshot: 2026-09-21 **17:26:45~17:26:52 KST**, 프로젝트79·요청125·초안235개.
- 실제 history REST GET: **17:27:36~17:27:45 KST**, 현재 작성 회차127개 전수, 동시4·nextPageToken 종료까지 조회, 모두 성공.
- 운영 CUA는 root가 재접속했으나 apps/browsers 빈 목록 및 native pipe 실패. 운영 인증 화면을 직접 조작한 검증은 아니다.
- 브라우저 증거는 배포 소스의 실제 컴포넌트와 새 운영 GET 데이터 또는 명시한 합성 fixture를 로컬에서 실행한 것이다. 모든 프로젝트를 육안 확인했다고 주장하지 않는다.
- 이 조사에서 운영 POST/PATCH/DELETE, 저장·복원·제출·승인을 실행하지 않았다. 토큰을 출력하지 않았으며 원문 GET 파일은0600이다.

## 임시저장 전수 결과

127개 최신 구조 초안 모두 소유자와revision이 존재한다. 활성 최신 구조66개 모두 payload가 존재한다. 구형107개 등록 문서는 새 payload/revision 구조가 아니며 별도로 분류했다. cashflow1개는 존재 집계만 하고 조사·변경 대상에서 제외했다.

| 현재 작성 회차 | 초안 수 | 실제 history 있는 초안 | revision 문서 수 |
|---|---:|---:|---:|
| 프로젝트 수정 | 66 | 2 | 15 |
| 프로젝트 등록 | 61 | 1 | 31 |
| 합계 | 127 | 3 | 46 |

등록 초안 `34f8e000-2940-4e87-8ba0-3413c3f47e80`은 실제rev2~32의31개 이력이 존재한다. 이 중30개에 저장자ID와저장시각이 기록되었다. snapshot에서는rev25였지만 이후 history GET에는rev32까지 있어 조회 사이에 변경이 계속된 사실도 확인된다. 이 조사에 의한 저장은 아니다. 비원자적 두 읽기를 같은 순간의 상태로 간주하지 않는다.

수정 초안2개는 각각rev386~397의12개,rev0~2의3개 이력이 있고 저장자·저장시각 메타데이터는 미기록이다. 구형 기록에 현재 저장자를 소급해 채운다고 해석하지 않는다.

나머지124개는 현재 회차history 문서가 없다. 그중활성63개는 현재 초안을1개로 보여주는 fallback이며 과거 내용을 비교·복원 가능하다는 증거가 없다. 이전generation 전체를 전수 조회한 것은 아니므로 다른 회차의 history까지 없다고 단정하지 않는다.

새 운영31개history를 실제 `ProjectDraftVersionPanel`에 재현하여 처음20개→더보기31개, 추가 버튼 종료, 가장 오래된rev2 선택/비교를 실행했다. 최신 저장자·시각과 구형 저장자 미기록이 구분되며JS예외0건이다. PNG를 직접 육안 확인했다. 페이지 데이터 공급은 로컬 callback이므로 실제 인증 history API pagination 호출을 했다는 뜻은 아니다.

## 상태·체크아웃 검토

배포된 `deriveProjectStatusFromContractPeriod`를2026-09-21 기준으로79개 원장에 직접 실행했다. 원장 객체 변경 없음도 검증했다.

- 기간 기준 제안 상태와 저장 상태가 다른 것은27개.
- 휴지통8개를 제외한 실제 목록 대상은 **19개**다.
- 진행중→기간종료14개, 계약전→진행중2개, 완료→기간진행중2개, 계약전→기간종료1개.

이는 자동 변경 대상이라는 뜻이 아니다. 완료(잔금대기) 보존과 종료미정 조건은 실제 공통 helper 규칙을 따랐다. 정정은 담당자 확인 후 초안 수정→제출→승인 절차를 거쳐야 하며, 조사로 원장이나 체크아웃 상태를 변경하지 않았다. KDB `p1780662870530`의 원장 완료/기간진행중 차이도 아직 남아 있다.

이전 조사에서 PENDING이었던 `change-p1773817948751`은 새 자료에서WITHDRAWN이다. 예전승인대기 버전충돌을 현재 활성 승인 장애로 반복 보고하지 않는다.

## 필수값 정책 전수 실행

새 운영79개원장과125개제출 snapshot에 배포된 `projectSubmissionCompletenessIssues`를 실행했다.125개 모두 신규 제출 기준으로는1개이상 보완 항목이 있다. 상태별로승인87·철회2·대기27·반려9건이다. 필수값 정책 전체 충족 상태라고 보고할 수 없다.

다만 현재 신규 제출 규칙을 구형 제출본에 진단 적용한 결과다. 기존승인87개를 무효로 하거나 모든 대기27개의 승인이 무조건 실패한다는 뜻은 아니다. 실제승인 readiness는 버전·첨부·구형호환 등 별도 경로를 사용한다.

대표 보완 항목은 보험/퇴직금 확인 응답과 기타인력의 명시적없음 등이며, 이자반납 필수조건 관련 항목은62개제출본에서 나타났다. 모든값을 기본0/없음으로 채우는 보정은 하지 않았다.

배포 소스로 합성 fixture 실제Wizard 회귀도 새로 실행했다. 조건부 이자6경우, 기존사업비방식Portal표시, 선택서류4종 해당없음, 필수응답7종·false3종 보존, 링크입력/체크해제보존, 버전비교5경우 모두통과(JS예외0). 이는 정책동작 회귀이며 운영125개가 보완되었다는 증거가 아니다.

## 남은 한계와 개선 후보

- 운영 초안63개의 과거history 부재는 이번기능배포만으로 자동해결되지 않는다. 보관시작 이후의 실제 이력만 비교·복원한다고 안내해야 한다.
- 상태차19개와 구형 필수응답 미기록은 담당자확인이 필요하다. 임의정정/자동확정은 하지 않는다.
- 실제history 비교화면의 `contractAnalysis` 깊은 필드는model/provider/confidence 같은 내부키가 그대로 나타나 길어진다. 비교기능은 동작하지만 비개발자에게 읽기쉽게 설명하는 개선 여지가 있다.
- 라이브authenticatedPortal에서 복원쓰기와 승인동작을 실행하지 않았다. 운영화면직접확인과 운영history GET/로컬재현을 구별해야 한다.

증거: `/tmp/myscube-805-draft-evidence/corpus.json`, `corpus.log`, `history-raw.json`, `history-summary.json`, `fresh-history-ui.json`, `fresh-history.png`, `interest/`, `required/`. snapshot은 `/tmp/myscube-805-audit/evidence.json`. 코드경로는 독립 추출 `/tmp/myscube-805-qa`이며 제품수정 없음.
