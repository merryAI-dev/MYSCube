# 첨부 참조와 실제 객체 대조 — 2026-09-21

## 범위와 결과

10:14 KST에 수집한 프로젝트 79개·요청 125개·초안 233개의 첨부 참조 950회에서 저장 경로를 중복 제거한 209개를 대상으로, 11:29 KST에 Google Cloud Storage JSON API의 객체 metadata GET을 수행했다. 기존 gcloud 인증을 사용했으며 파일·레코드·권한을 변경하지 않았다. 공개 다운로드 토큰을 보고서나 명령 출력에 노출하지 않았다.

- 208개: HTTP 200, 실제 객체 존재.
- 존재하는 208개: 저장된 크기·contentType 주장과 실제 객체 metadata가 모두 일치.
- 1개: HTTP 404. 해당 참조는 `projectRequestDrafts/321b0993-4e00-4386-9522-532c1337a571` 하나에만 존재하며 초안 상태는 DISCARDED, 폐기 시각은 2026-08-26이다. 따라서 현재 제출/승인 첨부가 유실된 사례로 분류하지 않는다.
- 원장 참조 경로 155개, 요청 참조 경로 185개, privateEditDraft 참조 경로 80개, registration draft 참조 경로 57개를 포함한다. 컬렉션 간 중복이 있어 합계는 209와 다르다.

## 의미와 한계

확인된 승인 페이지의 미제출/누락 표시를 곧바로 '파일 자체가 사라졌다'고 설명할 근거는 없다. 이번 캡처에서 원장·요청이 참조하는 객체는 모두 존재했다. 다만 저장 snapshot이 선택한 요청과 맞는지, BFF가 해당 사용자에게 올바른 참조를 돌려주는지, UI가 이를 표시하는지는 별도로 검증해야 한다.

이 검사는 객체 존재·크기·MIME metadata 확인이다. 파일 본문의 의미, 모든 PDF/Office 페이지, 직원/조직장별 다운로드 권한, signed URL 만료 및 브라우저 미리보기 성공을 검증한 것은 아니다. 파일명을 읽을 수 있는 것과 해당 사용자가 파일을 열 수 있는 것을 구분한다. 10:14 이후 새로 업로드된 파일은 이번 검사 분모에 포함되지 않는다.

비공개 원시 결과: `/tmp/myscube-review-audit-20260921/storage-attachment-audit.json` (0600). 경로·개인정보가 포함될 수 있으므로 저장소에 커밋하지 않는다.
