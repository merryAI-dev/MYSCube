const common = `import React,{useState} from 'react';`;
export const remoteDomBenchmarkWorkspaces = [
  { title: '표 필터', code: `${common}export default function App(){const[q,setQ]=useState('');const[n,setN]=useState(0);const rows=Array.from({length:40},(_,i)=>({name:'사업 '+(i+1),amount:(i+1)*100000}));return <main className="p-6 bg-slate-50 font-sans"><h1 className="text-2xl font-bold mb-4">사업 입금 현황</h1><label className="block mb-4">사업 검색<input aria-label="벤치마크 입력" className="block border p-2 rounded" value={q} onChange={e=>setQ(e.target.value)}/></label><button className="bg-blue-600 text-white p-2 rounded" onClick={()=>setN(v=>v+1)}>새로 계산</button><p role="status">{n}|{q}</p><table className="w-full border-collapse"><thead><tr><th className="text-left p-2 border-b">사업</th><th className="text-left p-2 border-b">입금액</th></tr></thead><tbody>{rows.filter(r=>r.name.includes(q)||q.startsWith('sample')).map(r=><tr key={r.name}><td className="p-2 border-b">{r.name}</td><td className="p-2 border-b">{r.amount.toLocaleString()}원</td></tr>)}</tbody></table></main>}` },
  { title: '업무 폼', code: `${common}export default function App(){const[q,setQ]=useState('');const[n,setN]=useState(0);const[priority,setPriority]=useState('normal');return <main className="p-6 bg-white font-sans"><h1 className="text-2xl font-bold mb-4">업무 요청서</h1><form onSubmit={e=>{e.preventDefault();setN(v=>v+1)}}><fieldset className="border rounded p-4"><legend className="font-bold">요청 정보</legend><label className="block mb-4">담당자<input aria-label="벤치마크 입력" className="block border p-2 rounded" value={q} onChange={e=>setQ(e.target.value)}/></label><label className="block mb-4">우선순위<select className="block border p-2" value={priority} onChange={e=>setPriority(e.target.value)}><option value="normal">보통</option><option value="high">높음</option></select></label><button type="button" className="bg-blue-600 text-white p-2 rounded" onClick={()=>setN(v=>v+1)}>새로 계산</button></fieldset></form><p role="status">{n}|{q}</p><p className="text-slate-600">입력된 담당자와 우선순위를 확인한 뒤 요청을 제출합니다.</p></main>}` },
  { title: '상태 대시보드', code: `${common}export default function App(){const[q,setQ]=useState('');const[n,setN]=useState(0);return <main className="p-6 bg-slate-50 font-sans"><h1 className="text-2xl font-bold mb-4">팀 업무 현황</h1><label className="block mb-4">팀 필터<input aria-label="벤치마크 입력" className="block border p-2 rounded" value={q} onChange={e=>setQ(e.target.value)}/></label><section className="grid grid-cols-3 gap-4">{[{label:'진행 중',value:12},{label:'확인 필요',value:3},{label:'완료',value:28}].map(item=><article className="bg-white border rounded p-4" key={item.label}><h2 className="font-bold">{item.label}</h2><p className="text-2xl">{item.value+n}</p></article>)}</section><button className="bg-blue-600 text-white p-2 rounded mt-4" onClick={()=>setN(v=>v+1)}>새로 계산</button><p role="status">{n}|{q}</p><ul className="p-4">{['기획 검토','자료 확인','실행 결과 공유'].map(item=><li key={item}>{item}</li>)}</ul></main>}` },
];
